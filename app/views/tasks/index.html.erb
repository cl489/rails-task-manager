<h1>ALL YOUR TASKS</h1>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task) %></h2>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
  <%= link_to "remove task", task_path(task), method: :delete %>
<% end %>

<h2><%= link_to "Add a new task", "tasks/new" %></h2>
